# Config

This folder contains editable application configuration.

`config.json` stores default and saved UI settings such as selected model name, dataset paths, output paths, device preferences, Live tab settings, and Training tab parameters.

Before sharing the repo publicly, keep machine-specific paths empty unless they are intentional examples. Runtime users can update these settings through the app UI.
